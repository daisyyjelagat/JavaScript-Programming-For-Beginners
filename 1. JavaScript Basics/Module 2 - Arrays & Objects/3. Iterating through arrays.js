//iterating through an array using a for loop

const fruits = ['apple', 'banana', 'orange', 'strawberry'];
console.log(fruits)
for (let i=0; i< fruits.length; i++){
    console.log(fruits[i]); // accesses each element in the array and logs it onto the console
}